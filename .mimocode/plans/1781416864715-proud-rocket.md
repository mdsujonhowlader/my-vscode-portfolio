# Plan: Implement blog detail page

## Goal
Make `app/blogs/[id]/page.js` display the full blog post (title, image, date, content) instead of just the ID.

## File to modify
`/home/sujon/Dev/my-vscode-portfolio/app/blogs/[id]/page.js`

## Implementation
1. Import `blogData` from `@/static/blog-data`
2. Find the blog by `id` from params
3. Handle not-found case (return 404 if blog doesn't exist)
4. Render: hero image, title, date, content paragraphs (split content by `\n\n` into paragraphs)
5. Style consistently with existing pages using `var(--color-*)` CSS variables

## Content rendering
The `content` field is a template literal string with paragraphs separated by `\n\n`. Split on `\n\n` and render each as a `<p>` tag.

## Verification
- `npm run build` to check for errors
- Visit `/blogs/1` and confirm full blog renders with image, title, date, and all paragraphs
